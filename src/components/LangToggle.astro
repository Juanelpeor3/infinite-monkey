---
import type { Lang } from "../i18n/translations";

interface Props {
    lang: Lang;
}

const { lang } = Astro.props;
const path = Astro.url.pathname;

let altUrl: string;
if (lang === "es") {
    altUrl = "/en" + (path === "/" ? "" : path);
} else {
    altUrl = path.replace(/^\/en/, "") || "/";
}

const altLabel = lang === "es" ? "EN" : "ES";
---

<a
    href={altUrl}
    class="text-xs font-sans tracking-widest uppercase text-[#8b6f47] border border-[#d5c8b5] px-3 py-1 rounded-full hover:bg-[#8b6f47] hover:text-white hover:border-[#8b6f47] transition-colors"
>
    {altLabel}
</a>
